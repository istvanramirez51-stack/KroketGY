# NEXT_PUBLIC_API_URL: Direct URL of the decoupled PHP MySQL backend API
# If empty, the application will fallback to high-performance localStorage-based persistence so testing can proceed immediately.
NEXT_PUBLIC_API_URL="http://jedakuliah.freehosting.dev"
